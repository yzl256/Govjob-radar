# 体制内岗位推荐系统 · 设计文档

> 版本 v0.2 · 基于《研究生进体制内，这10条路你一定要知道！》梳理的路径体系
> 一句话定位：用户填写个人档案，系统每日抓取 10 条体制内路径的招聘公告，做资格匹配后推送"你能报的岗位"。

---

## 0. 决策记录（v0.2 已锁定）

| # | 议题 | 结论 | 设计影响 |
|---|---|---|---|
| D1 | 产品形态 | **GitHub 开源 · 个人自用工具**（MIT） | 单/少用户优先，无多租户；管理后台降级为「YAML 配置 + CLI + H5 复核页」；docker-compose 一键部署 |
| D2 | 推送渠道 | **H5 主界面 + 微信推送适配器（Server酱/PushPlus/wxpusher）+ 邮件兜底** | 个人主体无法注册服务号（详见 §10.0），用适配器实现"消息进微信"，未来有主体再加服务号适配器 |
| D3 | C 类源站范围 | **用户在档案勾选省份/城市，按需启用对应源站**；首批订阅省：**广东、山东、浙江** | 源站注册表按省组织（`config/sources/*.yaml`，骨架已建），未勾选的省不抓，抓取量与 LLM 成本随选择伸缩 |
| D4 | 技术栈 | Python + FastAPI + PostgreSQL，docker-compose 部署 | 已确认 |

---

## 1. 产品定位

- **解决什么问题**：体制内招聘信息散落在数百个官网（组织部、人社厅、军队人才网、国聘网、区县政府网……），发布时间不规律、截止时间短（常见 5~10 天报名窗口），毕业生手动盯不过来，经常"看到时已截止"。
- **核心价值**：不是"信息聚合"（那只是爬虫），而是**资格匹配**——告诉用户"这个岗位你能不能报、为什么、哪天截止"。
- **目标用户**：作者本人为主 + 有同样需求的硕博毕业生（自助部署，GitHub 开源）。
- **非目标（v1 不做）**：笔试面试辅导、竞争比预测、代办报名、多租户 SaaS、支付。

---

## 2. 总体架构

```
┌─────────────────────────────────────────────────────────┐
│  用户端（自部署 H5）                                      │
│   档案填写 · 岗位列表 · 三态匹配详情 · 收藏/不感兴趣        │
│   设置：推送渠道、订阅省份（D3）                            │
└──────────────┬──────────────────────────────────────────┘
               │
┌──────────────▼──────────────────────────────────────────┐
│  应用层：档案服务 │ 匹配引擎 │ 推送适配器(多渠道) │ 运维CLI  │
│  （无独立管理后台：源站配置=YAML，复核队列=CLI+H5 页）      │
└──────┬───────────────▲──────────────┬───────────────────┘
       │               │              │
┌──────▼──────┐ ┌──────┴───────┐ ┌────▼────────────────┐
│  抓取层      │ │   数据层      │ │  知识库层            │
│  调度器      │ │  PostgreSQL  │ │  专业代码目录(3套)    │
│  Excel下载器 │ │  + ES/向量检索│ │  双一流院校名单       │
│  HTML爬虫    │ │  (岗位/用户/  │ │  行政区划码           │
│  LLM抽取器   │ │   匹配/源站)  │ │  岗位写法解析规则     │
└──────┬──────┘ └──────────────┘ └─────────────────────┘
       │
┌──────▼─────────────────────────────────────────────────┐
│  数据源：A类 Excel职位表 │ B类 平台型网站 │ C类 散落公告   │
└─────────────────────────────────────────────────────────┘
```

每日流水线（cron，建议 06:00 / 12:00 / 21:00 三班）：

```
源站健康检查 → 抓取列表页 diff（发现新公告）→ 下载/抓取详情
→ 去重(content_hash) → 字段抽取(Excel解析 or LLM) → 规则校验入库
→ 增量匹配（新岗位 × 存量用户）→ 生成日报 → 推送
```

---

## 3. 用户档案模型

### 3.1 用户提出的 5 个字段（保留并细化）

| 字段 | 存储建议 | 说明 |
|---|---|---|
| 学历 | enum: 大专/本科/硕士/博士(含在读年级) | 岗位有"仅限本科/本科及以上/硕士及以上"多种形态，需配合"毕业时间"推应届 |
| 专业 | 专业名 + 自动带出代码 | 用户输入专业名，系统联想出代码（不要让用户手填代码，易错） |
| 专业代码 | 按学历层次绑定对应目录 | 本科→本科目录；硕士→研究生学术/专业学位目录（两套都要存） |
| 年龄 | **存出生日期，不存年龄** | 岗位写法是"1998年7月以后出生"，存出生日期才能长期正确匹配 |
| 性别 | enum | 仅用于匹配依法限定性别的岗位（如女子监狱管教岗）；同时可标记涉嫌性别限制的岗位供用户留意 |

### 3.2 必须补充的字段（否则一半路径无法匹配）

| 字段 | 为什么必需 | 影响路径 |
|---|---|---|
| 毕业时间 + 应届身份（应届/往届/择业期内未落实工作） | 选调生只招应届；校招类国企多数只招应届 | 选调生、国企央企 |
| 政治面貌（党员/预备/团员/群众） | 选调生普遍要求党员或预备党员 | 选调生、部分人才引进 |
| 学校及层次（双一流/985/211/普通/海外QS排名） | 名校优生选调限双一流；军队文职免笔试限双一流；人才引进补贴分档 | 选调生、军队文职、人才引进 |
| 学生干部经历、校级以上表彰 | 选调生常见硬门槛 | 选调生 |
| 生源地 / 户籍 | 部分省考岗位、人才引进限本地户籍或生源 | 省考、人才引进、社区工作者 |
| 意向地域（省/市多选） | 排序与过滤的核心偏好 | 全部 |
| 意向路径（10 条多选 + 权重） | 用户分流，降低推送噪音 | 全部 |
| 其他证书（四六级、教资、法考、CPA 等） | 部分岗位硬性要求 | 特岗教师（教资）、涉外岗（六级） |

### 3.3 档案完整度引导

匹配引擎输出"⚠️信息不足"时，正是引导用户补全档案的时机（如："补充政治面貌后，为你解锁 23 条选调生岗位"）。

---

## 4. 岗位统一数据模型

10 条路径的公告异构性极强，入库前必须归一到同一 schema：

```sql
CREATE TABLE jobs (
  id             BIGSERIAL PRIMARY KEY,
  path           SMALLINT NOT NULL,      -- 1选调生 2国考 3省考 4事业单位 5人才引进
                                          -- 6国企央企 7军队文职 8三支一扶 9特岗/西部计划 10辅导员/社区
  source_site    TEXT NOT NULL,          -- 抓取来源站
  source_url     TEXT UNIQUE NOT NULL,   -- 官方公告原文链接
  title          TEXT NOT NULL,
  employer       TEXT,                   -- 用人单位
  region_code    VARCHAR(12),            -- 行政区划码（省/市/县）
  publish_date   DATE,
  apply_start    DATE,                   -- 报名开始
  apply_deadline DATE,                   -- 报名截止（匹配与提醒的关键字段）
  -- ↓ 资格约束（匹配引擎的输入）
  edu_require    JSONB,      -- {"min":"硕士","max":null} 或 {"exact":"仅限本科"}
  major_rules    JSONB,      -- [{"type":"code","value":["0812","0854"]},{"type":"text","value":"计算机相关"},{"type":"any"}]
  birth_after    DATE,       -- 出生时间下界（由"1998年7月以后出生"解析而来）
  age_max        SMALLINT,   -- 或年龄上限（二者通常只需其一）
  gender_limit   SMALLINT,   -- 0不限 1限男 2限女
  political_req  SMALLINT,   -- 0不限 1党员(含预备) 2党员或团员
  fresh_only     BOOLEAN,    -- 是否限应届
  household_req  JSONB,      -- 户籍/生源限制
  other_reqs     JSONB,      -- [{type:"cet6"},{type:"teacher_cert"},{type:"cadre_exp"}]
  -- ↓ 展示与排序
  exam_type      SMALLINT,   -- 1笔试 2免笔试直接面试
  quota          INT,        -- 招录人数
  org_type       TEXT,       -- 编制/单位性质：公务员/参公/全额拨款事业/差额/自收自支/央企/军队文职…
  highlights     TEXT,       -- 待遇亮点摘要（安家费、补贴、年薪）
  summary        TEXT,       -- LLM 生成的一段话摘要
  raw_html_path  TEXT,       -- 原始页面存档（对象存储）
  content_hash   TEXT,       -- 去重指纹
  extracted_by   SMALLINT,   -- 1Excel解析 2平台结构化 3LLM抽取
  confidence     SMALLINT,   -- 抽取置信度 0-100
  status         SMALLINT,   -- 1正常 2疑似过期 3已截止
  created_at     TIMESTAMPTZ DEFAULT now()
);
```

要点：
- **资格字段全部结构化存储**，匹配引擎不读原文；
- `confidence` 低的岗位（LLM 抽取不确定）标记给人工复核，同时匹配结果提示"以原文为准"；
- `apply_deadline` 是仅次于资格的核心字段——日报和临期提醒都靠它。

---

## 5. 数据源普查与分级（架构的根本依据）

### A 类：官方结构化职位表（Excel 附件）——字段全、解析成本最低

| 路径 | 数据源 | 说明 |
|---|---|---|
| 国考 | 国家公务员局考试录用专题网站（bm.scs.gov.cn） | 每年 10 月左右发布职位表 Excel，含学历/专业代码/政治面貌/应届/性别/备注全字段 |
| 省考 | 各省人事考试网 / 公务员局官网 | 多数省发 Excel 职位表；多数省份参加 3~4 月联考，节奏固定 |
| 军队文职 | 军队人才网（81rc） | 发布统一职位表 Excel，含专业代码、来源类别（双一流免笔试规则） |

### B 类：平台型招聘网站——有站内检索，结构半统一

| 路径 | 数据源 | 说明 |
|---|---|---|
| 国企央企 | 国聘网（iguopin.com，国资委背景）+ 央企官网校招栏目 | 国聘聚合了大量央企岗位，字段较规范 |
| 高校辅导员/行政 | 高校人才网（gaoxiaojob.com）、各高校人事处 | 高校人才网聚合度高 |
| 特岗教师 | 教育部特岗计划招聘平台 + 各省教育厅 | 每年一次，计划数固定（2026 年 8954 名），公告型 |
| 西部计划 | 西部计划官网（xibu.youth.cn，共青团系统） | 每年招募（2026 年 5 万名），公告型 |

### C 类：散落在区县/地市官网的 HTML 公告——数量最大、最乱、价值也最高

| 路径 | 数据源 | 难点 |
|---|---|---|
| 选调生 | 各省委组织部官网（如江苏先锋网等，各省站名不同） | 一年一次，窗口短（几天），错过即无 |
| 事业单位 | 各省人社厅 + 各地市/区县人社局公告栏 | 全国数千个发布点，写法五花八门 |
| 人才引进 | 各地人社局/组织部/人才办官网 | 同上，且常无职位表只有公告正文 |
| 三支一扶 | 各省人社厅专栏 | 省级聚合，难度中等 |
| 社区工作者 | 各区县政府/民政/街道官网 | 最下沉、最分散 |

> ⚠️ 站点清单必须在第 1 周做一次人工普查核实（URL、更新频率、是否有反爬、robots 声明），本表为方向性指引。

**结论：A 类优先做（2 周内可跑通端到端），B 类次之，C 类用 LLM 抽取管线逐步接入。**

---

## 6. 抓取系统设计

### 6.1 调度

- 三级调度：A 类按公告季高频盯梢（国考发布期每小时一次）；B 类每日 2~3 次；C 类源站每日 1~2 次，深夜低频。
- 每个源站一条配置记录（URL、列表页 CSS/XPath、更新频率、健康状态），配置即 YAML 文件（`config/sources/*.yaml`），改完重启生效——**源站改版是日常，配置化比改代码重要**。
- **按省启用（D3）**：源站注册表按省/市组织；调度器只运行「用户档案已勾选地区」的 C 类源站 + 全国性源站（A/B 类天然全国）。用户改选省份，下个调度周期自动增减抓取任务——抓取量与 LLM 成本随选择线性伸缩。

### 6.2 增量与去重

- 列表页 diff（新出现的链接）→ 详情抓取 → `content_hash`（正文规范化后哈希）判重；
- 同一公告被多个转载站发布：按标题+地区+截止日期做二级模糊去重，保留官方源。

### 6.3 抓取合规红线（写死在制度里）

1. 只抓公开公告页，**不破解、不绕过任何验证码或风控**（微信这类拦截直接放弃，换官方源）；
2. 遵守 robots.txt 与站点条款，单站限速（如 ≤ 6 req/min，加随机间隔）；
3. 存储**摘要 + 结构化字段 + 原文链接**，推送内容引导用户回官方原文报名——既尊重版权也降低合规风险；
4. UA 标识可联系身份（部分政府站可留联系方式备案）；
5. 用户数据遵守《个人信息保护法》：最小收集、明示同意、可导出可删除、出生日期等敏感字段加密。

---

## 7. 字段抽取管线

### 7.1 A 类：Excel 职位表解析器

- 公务员职位表列名每年微调，做列名映射配置 + 版本适配；
- 专业列写法如 `"0812计算机科学与技术、0854电子信息"` 或 `"计算机类(0809)"`，进入统一的专业规则解析器（见 §8）。

### 7.2 B 类：平台结构化抽取

- 每平台写适配器（列表分页、字段定位），输出统一 schema。

### 7.3 C 类：LLM 抽取 + 规则校验

- 对公告 HTML 正文调用 LLM，按 JSON Schema 抽取全部资格字段（这正是 LLM 擅长的：非结构化长文本 → 结构化）；
- 规则校验层兜底：出生日期解析（"1998年7月以后出生"→`birth_after=1998-07-01`）、日期合法性、行政区划码校验、置信度打分；
- 成本控制：只对**新增**公告跑 LLM，结果缓存永久复用；置信度 < 阈值进人工复核队列（CLI 命令导出 + H5 复核页，个人工具不做独立后台）。

---

## 8. 专业代码知识库（最大技术资产）

### 8.1 三套目录

| 目录 | 适用 | 示例 |
|---|---|---|
| 《普通高等学校本科专业目录》 | 本科 | 080901 计算机科学与技术 |
| 《研究生教育学科专业目录》学术学位 | 学硕 | 0812 计算机科学与技术（一级学科，工学 08 门类下） |
| 专业学位目录 | 专硕 | 0854 电子信息 |

配套数据表：**双一流高校名单**（142 所左右，含分学科名单）、行政区划码表。

### 8.2 岗位专业写法解析器（规则资产，需持续沉淀）

岗位写法归纳为 5 种，解析成统一 `major_rules`：

| 写法 | 解析结果 |
|---|---|
| `专业不限` / 不提 | `{"type":"any"}` |
| `0812计算机科学与技术` | 具体代码 |
| `计算机类(0809)` / `08 大类` | 类/门类代码 → 展开为该前缀下所有专业 |
| `计算机、软件相关` | 文本别名 → 映射到代码集合（别名表持续维护） |
| `限XX专业及相近` | 代码 + 相近专业白名单（人工维护） |

### 8.3 匹配算法

```
用户专业代码 u（可能双目录：本科代码 + 研究生代码）
岗位规则 r：
  any        → ✅
  code 精确  → u == code ?
  code 前缀  → u.startswith(prefix) ?   # 类/门类展开
  文本别名   → u ∈ 别名映射集合 ?
用户拿当前最高学历报考时用对应目录的代码；同时支持"本科专业也可用于专科可报岗"的场景。
```

---

## 9. 匹配引擎

### 9.1 硬过滤（资格约束，全过才算 ✅）

| 约束 | 用户字段 | 岗位字段 |
|---|---|---|
| 学历 | 学历 | edu_require（min/max/exact） |
| 专业 | 专业代码 | major_rules |
| 年龄 | 出生日期 | birth_after / age_max |
| 性别 | 性别 | gender_limit |
| 应届 | 毕业时间+应届状态 | fresh_only |
| 政治面貌 | 政治面貌 | political_req |
| 户籍/生源 | 户籍/生源地 | household_req |
| 其他 | 证书、干部经历 | other_reqs |

### 9.2 三态输出（可解释是产品核心）

```
✅ 可报     —— 全部硬约束通过
⚠️ 信息不足 —— 已知约束全过，但档案缺字段无法判定（如岗位要党员，用户没填政治面貌）
❌ 不可报   —— 附具体原因："专业不符（岗位要求 0812/0854，你的专业是 0301）"
```

每条匹配结果带 `reasons` 数组，前端逐条展示判定依据。**用户对"为什么推荐/为什么不推荐"的信任，决定产品生死。**

### 9.3 软排序（同样可报的岗位怎么排）

```
score = w1·地域匹配(省市命中)
      + w2·路径偏好权重
      + w3·学历适配度（岗位"硕士及以上"且用户硕士 → 高分；
                        岗位本科即可、用户硕士 → 降权：学历溢价未体现）
      + w4·待遇亮点（安家费/补贴/薪酬命中关键词加权）
      + w5·紧迫度（距截止 ≤3 天提升，促使行动但不骚扰）
      + w6·新增度（24h 内新发布加权）

用户反馈闭环：不感兴趣/收藏/点击 回写，按路径和地域维度调 w。
```

### 9.4 性能

- 增量匹配：每日新岗位先按（地域+路径+学历）预分桶，桶内用户再细筛；档案变更时全量重算该用户。
- 个人工具单/少用户规模下，全量重算也是秒级——桶优化是为开源后多用户部署预留的。

---

## 10. 每日推送设计（D2：H5 + 微信推送适配器 + 邮件）

### 10.0 服务号可行性结论（为什么不用服务号）

- 微信**服务号**注册需要企业/个体工商户等组织主体，**个人主体无法注册**；个人只能注册订阅号——订阅号无模板消息/订阅通知权限，每日 1 次群发且被折叠在订阅号文件夹，不适合做提醒类推送。
- 替代路线：**推送适配器**。用户关注一个中介公众号（Server酱/PushPlus/wxpusher 均为此模式），系统调其 API，消息即以该公众号名义送达用户微信——零资质、免费额度对个人足够。
- 架构上做 `Notifier` 抽象接口 + 多适配器实现：若未来注册了主体，新增一个服务号适配器即可，核心不动。

### 10.1 渠道矩阵

| 渠道 | 接入成本 | 说明 |
|---|---|---|
| Server酱（Turbo） | 扫码拿 SendKey，约 30 秒 | **默认渠道**；免费额度每日 5 条，个人够用；消息直达微信 |
| PushPlus / wxpusher | 同样极简 | 备选，免费额度更大 |
| 企业微信群机器人 webhook | 建群加机器人即得 URL | 免费无门槛，需装企业微信 App |
| 邮件（SMTP） | 配 .env | 兜底渠道，永远保留（HTML 日报完整版） |

### 10.2 推送内容与频控

**每日一份早报（07:30）**：
1. 新增可报岗位 N 条（按路径分组，Top 展示，点击进 H5 详情）
2. ⏰ 临期提醒：3 天内截止 / 24 小时内截止（即时单独推送）
3. ⚠️ 待补全提示："补政治面貌解锁选调生岗位"
4. 周维度附：本周汇总 + 累计可报总数

**频控与分工**：除临期 24h 提醒外，每日至多一条主推送；微信渠道只推**摘要**（标题+数量+Top3），完整列表和三态判定详情留在 H5——既规避公众号通道的内容长度限制，也把"逐条看原因"留给 H5 交互。

---

## 11. 技术选型（务实版）

| 层 | 选型 | 理由 |
|---|---|---|
| 爬虫 | Python + httpx + Playwright（JS 渲染站） | 政府站老技术多，需浏览器兜底 |
| 调度 | APScheduler（单机）→ Celery Beat（扩展后） | 先简单 |
| 抽取 | pandas（Excel）+ LLM API（C 类公告） | LLM 用结构化 JSON 输出 + Pydantic 校验 |
| 主库 | PostgreSQL（JSONB 存资格约束） | 匹配查询 `jsonb @>` 够用，先不上 ES |
| 检索 | v1 不做 ES，Postgres 全文检索够用 | 用户量起来再演进 |
| 后端 | FastAPI | 与爬虫同语言，Pydantic 共享 schema |
| 前端 | 自部署 H5（Vite + Vue3，响应式适配手机） | 服务号个人主体不可用（§10.0），H5 为主界面 |
| 推送 | Notifier 抽象 + Server酱/PushPlus/wxpusher/企微 webhook/SMTP 适配器 | 个人可用的"消息进微信"方案 |
| 部署 | docker-compose 一键启动（app + postgres + scheduler 三容器）；原始页面存档先存本地卷 | 个人工具不上 K8s、不依赖云对象存储 |

### 11.1 开源仓库结构（D1）

```
govjob-radar/                  # 名称待定，仅占位
├── README.md                  # 部署四步曲 + 效果截图/GIF
├── LICENSE                    # MIT
├── docker-compose.yml         # app + postgres + scheduler
├── .env.example               # LLM_API_KEY / SERVERCHAN_SENDKEY / SMTP_*（.env 入 .gitignore）
├── config/
│   ├── sources/               # 源站注册表，按行政层级组织（D3）
│   │   ├── national.yaml      #   全国性：国考/军队文职/国聘/特岗/西部计划
│   │   ├── 江苏省.yaml         #   每省一文件：选调生/省考/事业单位/人才引进/三支一扶/社区…
│   │   ├── _template.yaml     #   补新省份的模板（欢迎社区 PR 补省）
│   ├── majors/
│   │   ├── undergraduate.csv  #   本科专业目录
│   │   ├── academic.csv       #   学术学位目录
│   │   └── professional.csv   #   专业学位目录
│   ├── major_aliases.yaml     #   "计算机相关"→代码集合 别名表
│   └── app.yaml               #   推送渠道开关/频控/抓取限速
├── backend/                   # FastAPI（API + 匹配引擎 + 调度入口）
│   ├── app/{models,api,matching,notify,crawler,knowledge}/
│   ├── cli.py                 #   运维 CLI：健康检查/复核队列/手动匹配/导入职位表
│   └── tests/                 #   匹配引擎 50+ 用例
├── web/                       # H5 前端
└── docs/                      # 设计文档、抓取合规说明
```

三个要点：
1. **密钥只进 .env**（.env.example 提供模板），仓库不含任何敏感信息；
2. **源站清单与专业目录是数据文件而非代码**——开源后社区可 PR 补省份源站、补专业别名，这正是个人工具长期可持续的护城河；
3. 抓取限速、robots 遵守等合规参数集中在 `app.yaml`，开源可见、默认保守。

---

## 12. MVP 路线图

| 阶段 | 周期 | 交付 |
|---|---|---|
| **P0 地基** | 第 1~2 周 | ✅ **已完成**（见 backend/）：专业代码知识库种子（3 套目录 CSV + 别名表 JSON）、用户档案模型、三态匹配引擎 + 51 个单测全绿。遗留：目录 import 脚本补全量、专科目录 |
| **P1 端到端** | 第 3~4 周 | ✅ **已完成**（见 backend/）：零依赖 xlsx 读写器、国考职位表解析器（表头定位+列别名+专业写法解析）、日报管道、Server酱/邮件/控制台推送适配器、CLI demo。86 个单测全绿；端到端演示通过（含跨目录类族匹配语义） |
| **P2 可日用** | 第 5~8 周 | ✅ **已完成**（见 backend/ + web/）：极简 H5（档案表单 + 多段学历 + 订阅省份勾选 + 三态结果与逐条判定依据）；调度核心（源站注册表 miniyaml 加载、全国恒启+按订阅省启用、健康记录落盘）；抓取器（UA 标识、同主机≥10s 限速、xlsx 附件发现下载，best-effort 降级）；`daily`/`serve`/`test-notify` CLI。115 个单测全绿。遗留：本机 TLS 全断（HTTP 可达，广东/山东入口页抓取成功），真实源联调待部署机；zj-shengkao 入口 404 待修 |
| **P3 攻长尾** | 第 9~16 周 | 🔄 进行中（**岗位级精度已打通**，160 单测全绿）：职位表附件展开上线——公告页 xlsx/zip 附件自动下载解析，广东选调 1421 岗 + 三支一扶 263 岗真实入库验证；通用职位表解析器（国考/省考/选调/事业/三支口径，双专业列按学历取列、年龄/户籍/党员/应届/资格证列、岗位代码内容寻址 id）；附件落 `data/attachments` 与用户 `inbox` 分离防二次解析；省级自编目录代码判 ⚠️ 不瞎猜。待办：山东/浙江源站普查、广州/深圳栏目页定位、zj-shengkao 404、省级目录（粤 A01/B02 系）知识库扩充 |
| **P4 开源发布** | 之后 | README/部署文档/demo GIF、社区 PR 补省份源站、软排序与反馈闭环 |

**P0/P1 是生死线：知识库和匹配引擎先行，爬虫只是供水管道。个人自用规模下，P2 结束即达到"每天真的在用"的状态，之后各阶段都是纯增量。**

---

## 13. 风险与待决问题

| 风险 | 对策 |
|---|---|
| 源站改版/反爬升级 | 源站配置化 + 健康检查告警 + 多源冗余（同一公告常有多站转载） |
| LLM 抽取错误导致误报资格 | 置信度分级 + 低置信人工复核 + 所有结果标注"以原文为准"+ 原文一键跳转 |
| 招聘淡季岗位量骤降（公务员考试一年就几场） | 日报改为周报节奏；淡季主打事业单位/国企/社区等常年招聘源 |
| 公告季洪峰（10 月国考、3~4 月联考） | 提前压测；洪峰期只保证 A 类时效 |
| 用户档案造假/误填 | 关键字段（专业代码）用目录联想而非手填；不承诺报名资格，免责声明 |

**决策状态**：产品形态 / 推送渠道 / C 类省份策略 / 技术栈四项已于 v0.2 锁定（见 §0 决策记录）。

剩余待办（非阻塞，随做随定）：
1. 首批源站注册表覆盖哪几个省——由你在 H5 勾选订阅省份即生效，`_template.yaml` 支持随时补省；
2. LLM 抽取预算上限——按省启用后量级大幅下降，每条公告约 2~4k token，个人月成本预计几元至几十元级。

---

## 附录 A：10 条路径 × 关键资格速查

| 路径 | 学历 | 应届 | 政治面貌 | 年龄参考 | 数据级别 |
|---|---|---|---|---|---|
| 选调生 | 本科起，硕士为主 | **仅应届** | 常要求党员 | 硕士约 28 岁内（各省不同） | C |
| 国考 | 岗位多为本科起，部委多要硕士 | 部分岗限应届 | 部分岗要求 | 一般 35 岁内，应届硕博放宽至 40 | A |
| 省考 | 同国考 | 部分岗限应届 | 部分岗要求 | 一般 35 岁内 | A |
| 事业单位 | 大专起 | 不限为主 | 少量要求 | 研究生约 38 岁内 | C |
| 人才引进 | 硕博为主 | 不限 | 少量要求 | 各地不同 | C |
| 国企央企 | 本科起 | **校招限应届为主** | 基本不限 | 校招应届为主 | B |
| 军队文职 | 本科起 | 不限 | 部分要求 | 一般 35 岁内 | A |
| 三支一扶 | 大专起 | 多限应届 | 不限 | 研究生约 28 岁内 | C |
| 特岗教师 | 本科起+教资 | 应届往届均可 | 不限 | 一般 30 岁内 | B |
| 西部计划 | 大专起 | 应届为主 | 团员/党员优先 | 一般应届 | B |
| 辅导员/社区 | 辅导员硕士起；社区大专起 | 辅导员应届往届均多 | 辅导员常要求党员 | 各地不同 | B/C |
